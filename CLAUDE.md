# CLAUDE.md — VPS Scripts

This is the VPS initialization toolkit and documentation site.

## Agent skills

### Issue tracker

Issues are tracked on GitHub Issues (`LoveDoLove/vps-scripts`). See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default five-label vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
